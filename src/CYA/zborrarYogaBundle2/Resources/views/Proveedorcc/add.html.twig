{% extends 'layout.html.twig' %}

{% block body %}
    {{ parent() }}
    <div class="container-fluid cuenta">
        <div class="container cuenta-icon">
            <div class="row">
                <div class="col-sm-5">
                    <h2>Nuevo Mov. Cuenta Corriente con Proveedores</h2>
                    <a href="{{ path('cya_proveedor_index') }}" class="head-link">
        			        <h3>
        			           <span><i class="icon ion-android-arrow-dropleft back"></i></span>
        			            Regresar a listado de CC. Proveedores
        			         </h3>
        			    </a>
                </div>
            </div>
        </div>
    </div>

    
    {{ form_start(form, {'attr' : {'role' : 'form'}, 'action':'', 'method':'POST' }) }}
           
     <div class="container grey-input">
        <div class="row">
                   <div class="col-sm-6">
                        <div class="form-group">
                           <b> Seleccione Proveedor </b>  <br>
                            <select class="selectpicker"  data-live-search="true" id="proveedor" name="proveedor"  data-style="btn-primary" 
                            value="{{ app.request.get('proveedor') }}">
                               <option   value = "">Elija un Proveedor</option>
                             {% for pr in proveedores %}
                             
                                 <option  value="{{ pr.id }}">{{ pr.nombre }} </option>
                             
                                
                             {% endfor %}
                             
                             
                           </select>
                        </div>
                       
                    
                    
                    <div class="form-group">
                         Fecha Vencimiento
                         {{ form_widget(form.fechavencimiento, { 'attr' : { 'class' : 'form-control', 'placeholder' : 'Fecha de Vencimiento'} }) }}
                         <span class="text-danger"> {{ form_errors(form.fechavencimiento) }} </span>
                    </div>
                    
                    <div class="form-group">
                         Deuda $
                         {{ form_widget(form.deuda, { 'attr' : { 'class' : 'form-control', 'placeholder' : 'Deuda $'} }) }}
                         <span class="text-danger"> {{ form_errors(form.deuda) }} </span>
                    </div>
                    
                    
              
                    
                     <div class="form-group">
                         Nº Factura
                         {{ form_widget(form.factura, { 'attr' : { 'class' : 'form-control', 'placeholder' : 'Nº Factura'} }) }}
                         <span class="text-danger"> {{ form_errors(form.factura) }} </span>
                    </div>
                    
                      <div class="form-group">
                        Comentarios
                         {{ form_widget(form.tipo, { 'attr' : { 'class' : 'form-control', 'placeholder' : 'Comentarios'} }) }}
                         <span class="text-danger"> {{ form_errors(form.tipo) }} </span>
                    </div>
                    
                    
            </div>

            
            <div class="col-sm-12">
                
                {{ form_widget(form.save, {'label' : 'Agregar CC. con  Proveedor', 'attr': {'class': 'btn alumnos-btn'} }) }}
                 
                
            </div>
            {{ form_end(form) }}
         </div>
     </div>
{% endblock %}

